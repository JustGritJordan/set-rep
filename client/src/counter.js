import React, {useState} from "react";

function Counter(){
    const [count, setCount] = useState(0);
    
    return(
        <div>
            {/*counter here*/}
        </div>
    )
}

export default Counter;